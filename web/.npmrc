# GitHub Packages Registry for @evarisai scope
@evarisai:registry=https://npm.pkg.github.com

# Authentication token (set via environment variable or npm login)
# For CI/CD: Set NPM_TOKEN environment variable
//npm.pkg.github.com/:_authToken=${PERSONAL_NPM_TOKEN}

# For local development, you can also authenticate by running:
# npm login --scope=@evaris --registry=https://npm.pkg.github.com
# (Use your GitHub username and a personal access token with read:packages scope)
